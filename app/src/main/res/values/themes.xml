<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.PhonebookApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PhonebookApp" parent="Base.Theme.PhonebookApp">
        <item name="android:textViewStyle">@style/SofiaTextViewStyle</item>
    </style>

    <style name="SofiaTextViewStyle" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/sofia_pro</item>
    </style>
</resources>